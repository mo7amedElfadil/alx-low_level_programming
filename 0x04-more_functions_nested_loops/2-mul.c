/**
 * mul - multiplies two digits
 * @a: first integer to be multiplied
 * @b: second integer to be multiplied
 * Return:  1 (Success)
 *			0 (Failure)
 */
int mul(int a, int b)
{
	return (a * b);
}
