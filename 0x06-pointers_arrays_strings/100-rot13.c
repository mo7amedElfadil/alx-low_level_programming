#include "main.h"
/**
 * rot13 - function that encodes a string using rot13.
 *
 *		can only use one if and 2 loops
 *		cant use switch
 * @s: string to encode
 * Return: s
 */
char *rot13(char *s)
{
	int i = 0;
	/* char c; */

	while (*(s + i) != '\0')
	{


		/* c = *(s + i); */
		if (1)
		{
		}
				i++;
	}
	return (s);
}

